{
  "name": "Code Walkthrough",
  "layout": "layout-split",
  "modules": [
    { "type": "heading", "zone": "content", "data": { "text": "How It Works", "level": 2 } },
    { "type": "text", "zone": "content", "data": { "markdown": "1. **Import** modules\n2. **Define** the function\n3. **Handle** edge cases" } },
    { "type": "prompt-block", "zone": "stage", "data": { "content": "async function process(input) {\n  const reader = input.getReader();\n  while (true) {\n    const { done, value } = await reader.read();\n    if (done) break;\n    yield parse(value);\n  }\n}", "language": "javascript", "quality": "good" } }
  ]
}
